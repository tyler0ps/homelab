apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  - namespace.yaml
  # Pin to a specific ArgoCD release — bump this version when upgrading
  - https://raw.githubusercontent.com/argoproj/argo-cd/v2.14.3/manifests/install.yaml
